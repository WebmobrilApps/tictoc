import 'package:flutter/material.dart';



const bgColor = Color(0xff0B0B0B);
const whiteColor = Color(0xffFFFFFF);
const blackColor = Color(0xff000000);
const buttonColor = Color(0xffFE2C55);
const appBlackColor = Color(0xff0B0B0B);
const appGreyColor = Color(0xffADADAD);
const appBgColor = Color(0xffFFFFFF);
const greenColor = Color(0xff2FAB01);

class AppColors {
  ///100% — FF
  /// 95% — F2
  /// 90% — E6
  /// 85% — D9
  /// 80% — CC
  /// 75% — BF
  /// 70% — B3
  /// 65% — A6
  /// 60% — 99
  /// 55% — 8C
  /// 50% — 80
  /// 45% — 73
  /// 40% — 66
  /// 35% — 59
  /// 30% — 4D
  /// 25% — 40
  /// 20% — 33
  /// 15% — 26
  /// 10% — 1A
  /// 5% — 0D
  /// 0% — 00

  static const bgColor = Color(0xffEBF0F6);
  static const greenButtonColor = Color(0xffFE2C55);
  static const blueButtonColor = Color(0xff4579BD);
  static const blackColor = Color(0xFF000000);
  static const whiteColor = Color(0xFFFFFFFF);
  static const dividerColor = Color(0xFFD9D9D9);


  static Color dividerColorOpacity = const Color(0xFFD9D9D9).withOpacity(0.3);

}
